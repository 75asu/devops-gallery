apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-flaskapp.fullname" . }}
  labels:
    {{- include "my-flaskapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "my-flaskapp.selectorLabels" . | nindent 4 }}
